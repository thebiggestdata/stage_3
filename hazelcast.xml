<?xml version="1.0" encoding="UTF-8"?>
<hazelcast xmlns="http://www.hazelcast.com/schema/config"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://www.hazelcast.com/schema/config
           http://www.hazelcast.com/schema/config/hazelcast-config-5.5.xsd">

    <cluster-name>SearchEngine</cluster-name>

    <network>
        <port auto-increment="false">5701</port>
        <public-address>${HZ_PUBLIC_ADDRESS}</public-address>

        <join>
            <multicast enabled="false"/>
            <tcp-ip enabled="true">
                <member-list>
                    <member>${HZ_SEED_MEMBERS}</member>
                </member-list>
            </tcp-ip>
        </join>

        <!-- AÑADIR ESTO -->
        <interfaces enabled="true">
            <interface>0.0.0.0</interface>
        </interfaces>
    </network>

    <multimap name="inverted-index">
        <backup-count>2</backup-count>
        <statistics-enabled>true</statistics-enabled>
    </multimap>

    <multimap name="datalake">
        <backup-count>2</backup-count>
    </multimap>

    <map name="replication-count">
        <backup-count>2</backup-count>
        <time-to-live-seconds>86400</time-to-live-seconds> </map>

    <map name="datalake-storage">
        <backup-count>2</backup-count>
        <eviction eviction-policy="LRU" max-size-policy="FREE_HEAP_PERCENTAGE" size="20"/>
    </map>

    <map name="processed-documents">
        <backup-count>2</backup-count>
        <time-to-live-seconds>3600</time-to-live-seconds>
    </map>
</hazelcast>
