events {
    worker_connections 1024;
}

http {
    log_format upstreamlog '$remote_addr - $host '
                          '[$time_local] '
                          '"$request" $status '
                          'upstream: $upstream_addr '
                          'rt=$request_time '
                          'urt=$upstream_response_time '
                          'u_status=$upstream_status';

    access_log /var/log/nginx/access.log upstreamlog;


    upstream search_backend {
        least_conn;

        # Add the master IP and the slaves IPs, if more machines are added, add more lines.
        server <MASTER_IP>:7003 max_fails=10 fail_timeout=30s;
        server <SLAVE_IP>:7003 max_fails=10 fail_timeout=30s;
        server <SLAVE_IP>:7003 max_fails=10 fail_timeout=30s;
        # server <SLAVE_IP>:7003 max_fails=10 fail_timeout=30s;

        keepalive 64;
    }

    server {
        listen 80;

        location /search {
            proxy_pass http://search_backend;

            proxy_http_version 1.1;
            proxy_set_header Connection "";

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;

            proxy_connect_timeout 60s;
            proxy_send_timeout 60s;
            proxy_read_timeout 60s;

            proxy_next_upstream error timeout http_502 http_503 http_504;
        }

        location /health {
            proxy_pass http://search_backend;

            proxy_http_version 1.1;
            proxy_set_header Connection "";

            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;

            proxy_connect_timeout 5s;
            proxy_read_timeout 5s;
        }
    }
}

